# Copy to .env and fill in, or just rely on your normal AWS credential chain
# (aws configure / SSO / instance role) -- .env is optional.

AWS_REGION=us-west-2

# Only needed if you're not using aws configure / SSO / an instance role:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
