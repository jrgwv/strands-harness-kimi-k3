[pytest]
# Let test modules import the shared helpers with `from conftest import ...`.
pythonpath = .
markers =
    slow: end-to-end tests that run the full agent (costs tokens, ~2-3 min).
addopts = -ra
